{
  "buildCommand": "pnpm --filter @workspace/alma-jalisco run build && ls -R artifacts/alma-jalisco/ && pwd",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "artifacts/alma-jalisco/dist",
  "framework": null,
  "env": {
    "NODE_ENV": "production"
  }
}
